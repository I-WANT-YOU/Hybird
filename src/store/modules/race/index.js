import raceInfo from './raceInfo';

const state = {
};

const mutations = {
};

const actions = {
};

const modules = {
  raceInfo,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  modules,
};
